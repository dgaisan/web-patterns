# This is an web app template that comprises Express.js backend, React.js frontend, and MySQL Server.


## create a copy `.env.template`, rename it to `.env` and update environment variables accordingly  

## build MySQL image
- docker build -t myApp-mysql -f Dockerfile.mysql .

## In order to build webapp follow steps bellow:
- docker build -t myApp .
- docker run -p 5000:5000 myApp
- This will start the app on http://localhost:5000
